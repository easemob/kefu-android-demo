language: android
jdk: oraclejdk8
sudo: false

android:
  components:
    - tools
    - platform-tools
    - tools
    - build-tools-26.0.2
    - android-26
    - extra-android-m2repository
    - sys-img-armeabi-v7a-android-26

branches:
  only:
    - dev
  
before_install:
  - curl -OfsSL https://raw.github.com/easemob/kefu-scripts/master/sdk-travis.sh
  - chmod +x sdk-travis.sh
  - chmod +x gradlew

before_script:
  # Create and start an emulator for instrumentation tests.
  #- echo no | android create avd --force -n test -t android-16 --abi armeabi-v7a
  #- emulator -avd test -no-audio -no-window &
  #- android-wait-for-emulator
  #- adb shell input keyevent 82

  
script:
 - ./sdk-travis.sh :kefu-sdk:clean :kefu-sdk:sonarqube

 
cache:
  directories:
    - $HOME/.gradle
    - $HOME/.m2/repository
    - $HOME/.android/build-cache

notifications:
    slack: easemob:tYWZJo2ydkdNkexGpLSVFac5 

env:
   global:
     - SONAR_URL=http://sonar.easemob.com:9000
     - SONAR_USER=zhaoliang
     - SONAR_PASSWORD=sonarsdk 
